name: newsletter_client_actions
on: [push]
jobs:
  project:
    runs-on: ubuntu-latest
    steps:
      - 
        name: Checkout repo
        uses: actions/checkout@v3
      -
        name: Install libraries
        run: npm i -D
      - 
        name: Run ESLint
        run: npm run lint